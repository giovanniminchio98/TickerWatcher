{
  "_comment": "Fully autonomous post+reply decision-maker (src/triggers/ai_manager.py). One Claude call decides BOTH whether to post something original AND which candidate replies (if any) to send, given a full data snapshot -- see README's 'AI Manager' section. min_hours_between_calls plus max_calls_per_day bound it to roughly 5-10 calls/day even though the workflow itself runs hourly.",
  "model": "claude-sonnet-5",
  "min_hours_between_calls": 2.5,
  "max_calls_per_day": 8,
  "max_posts_per_day": 5,
  "max_replies_per_day": 15,
  "max_replies_per_call": 3,
  "max_reply_candidates_per_account": 5
}
